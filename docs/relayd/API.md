# API

`relayd` exposes an authenticated HTTP control-plane API under `/v1`.

## Quick start

```bash
export BASE_URL=http://127.0.0.1:8080
export AUTH_TOKEN=mytoken

curl -sS \
  -H "Authorization: Bearer ${AUTH_TOKEN}" \
  "${BASE_URL}/v1/allocations"
```

## Authentication

All endpoints require a bearer token:

```http
Authorization: Bearer <AUTH_TOKEN>
```

If the header is missing, malformed, or the token does not match, the server returns:

- `401 Unauthorized`
- body: `unauthorized`

## Conventions

- Base path: `/v1`
- Successful `POST`/`PUT`/`GET` responses use `Content-Type: application/json`
- Successful `DELETE` responses return `204 No Content` with an empty body
- Error responses are plain text, not JSON
- Allocation IDs are UUIDv7 strings generated by the server
- `host` accepts IP literals only, for example `127.0.0.1` or `::1`
- `GET /v1/allocations` and `GET /v1/ports` return rows ordered by `protocol`, then `port`

## Resource model

### Allocation object

```json
{
  "id": "018f0d5d-86d6-7e57-8bb5-c0b7c38e2f0e",
  "protocol": "tcp",
  "port": 10000,
  "created_at_ms": 1712822400000,
  "updated_at_ms": 1712822400000
}
```

### Binding object

```json
{
  "allocation_id": "018f0d5d-86d6-7e57-8bb5-c0b7c38e2f0e",
  "host": "127.0.0.1",
  "target_port": 8080,
  "effective_target_port": 8080,
  "effective_host": "127.0.0.1",
  "runtime_status": "active",
  "error_kind": null,
  "last_error": null,
  "created_at_ms": 1712822401000,
  "updated_at_ms": 1712822401000
}
```

### Compatibility aggregate (`GET /v1/ports`)

This endpoint remains available as an aggregate/compatibility read model. It keeps the legacy flat shape, but `target_port` may now be `null` when an allocation has no binding.

```json
{
  "id": "018f0d5d-86d6-7e57-8bb5-c0b7c38e2f0e",
  "protocol": "tcp",
  "port": 10000,
  "target_port": 8080,
  "host": "127.0.0.1",
  "effective_target_port": 8080,
  "effective_host": "127.0.0.1",
  "host_configured": true,
  "runtime_status": "active",
  "error_kind": null,
  "last_error": null,
  "created_at_ms": 1712822400000,
  "updated_at_ms": 1712822405000
}
```

### `runtime_status` values

- `active`
- `rejecting_no_host`
- `degraded_bind_failed`
- `degraded_apply_failed`
- `degraded_close_failed`

### `error_kind` values

- `bind_failed`
- `apply_failed`
- `close_failed`
- `restore_failed`
- `invalid_input`

## Primary endpoints

### Allocation endpoints

#### Create allocation

`POST /v1/allocations`

Request body:

```json
{
  "protocol": "tcp"
}
```

Success:

- `201 Created`
- body: allocation object

Notes:

- `protocol` must be `tcp` or `udp`
- An allocation can exist without any binding
- The relay port is picked automatically from `PORT_RANGE`

#### List allocations

`GET /v1/allocations`

Success:

- `200 OK`
- body: array of allocation objects

#### Get allocation

`GET /v1/allocations/{id}`

Success:

- `200 OK`
- body: allocation object

If the allocation is missing:

- `404 Not Found`

#### Delete allocation

`DELETE /v1/allocations/{id}`

Success:

- `204 No Content`

Semantics:

- Deletes the allocation
- Deletes the binding too when one exists

### Binding endpoints

#### Create or replace binding

`PUT /v1/allocations/{id}/binding`

Request body:

```json
{
  "host": "127.0.0.1",
  "target_port": 8080
}
```

Success:

- `200 OK`
- body: binding object

Semantics:

- Replaces the entire binding for the allocation
- If the allocation does not exist, returns `404`

#### Get binding

`GET /v1/allocations/{id}/binding`

Success:

- `200 OK`
- body: binding object

If the allocation has no binding:

- `404 Not Found`

#### Delete binding

`DELETE /v1/allocations/{id}/binding`

Success:

- `204 No Content`

Semantics:

- Removes the binding only
- Keeps the allocation
- A follow-up `GET /v1/allocations/{id}` still succeeds
- A follow-up `GET /v1/allocations/{id}/binding` returns `404`

## Compatibility endpoints

These endpoints remain available for legacy clients. New clients should prefer the allocation/binding endpoints above.

### Create legacy port record

`POST /v1/ports`

Request body:

```json
{
  "protocol": "tcp",
  "target_port": 8080
}
```

Success:

- `201 Created`
- body: compatibility aggregate object

Semantics:

- Creates an allocation
- Seeds a compatibility binding with `target_port`
- Until `host` is configured, `runtime_status` is typically `rejecting_no_host`

### Set target host (legacy)

`POST /v1/ports/target`

Request body:

```json
{
  "id": "018f0d5d-86d6-7e57-8bb5-c0b7c38e2f0e",
  "host": "127.0.0.1"
}
```

Success:

- `200 OK`
- body: compatibility aggregate object

### Update legacy port record

`POST /v1/ports/{id}`

Request body:

Any non-empty combination of:

```json
{
  "target_port": 9090,
  "host": "127.0.0.1"
}
```

Success:

- `200 OK`
- body: compatibility aggregate object

Validation:

- At least one of `target_port` or `host` must be present
- Empty body `{}` returns `400 Bad Request` with body `empty update`

### Delete legacy port record

`DELETE /v1/ports/{id}`

Success:

- `204 No Content`

Semantics:

- Deletes the allocation and any binding

### List aggregate compatibility view

`GET /v1/ports`

Success:

- `200 OK`
- body: array of compatibility aggregate objects

## Metrics

### Metrics endpoint

`GET /v1/metrics`

Return runtime counters and gauges as a single JSON object.

Success:

- `200 OK`
- body: JSON object with integer values

#### Current metric fields

- Core: `allocations_total`, `runtime_apply_total`, `restore_failures_total`, `restore_timeout_total`, `rejected_no_host_total`, `bind_fail_total`, `http_non_loopback_bind_total`
- TCP splice/copy: `tcp_splice_fast_path_total`, `tcp_copy_fallback_total`, `tcp_splice_attempt_total`, `tcp_splice_success_total`, `tcp_splice_fallback_total`, `tcp_splice_hard_failure_total`, `tcp_splice_fallback_forced_total`, `tcp_splice_fallback_unsupported_total`, `tcp_splice_fallback_runtime_error_total`
- TCP sessions: `tcp_session_create_total`, `tcp_session_close_total`, `tcp_session_event_total`, `tcp_session_worker_dispatch_total`, `tcp_session_worker0_dispatch_total`, `tcp_session_worker1_dispatch_total`, `tcp_accept_handoff_total`, `tcp_accept_handoff_worker0_total`, `tcp_accept_handoff_worker1_total`, `tcp_accept_handoff_worker2_total`, `tcp_accept_handoff_worker3_total`, `tcp_listener_accept_total`, `tcp_listener_accept_worker0_total`, `tcp_listener_accept_worker1_total`, `tcp_listener_accept_worker2_total`, `tcp_listener_accept_worker3_total`, `tcp_upstream_connect_total`, `tcp_upstream_connect_fail_total`, `tcp_active_sessions`
- UDP core: `udp_packets_in_total`, `udp_packets_out_total`, `udp_bytes_in_total`, `udp_bytes_out_total`, `udp_recv_errors_total`, `udp_send_errors_total`, `udp_session_create_total`, `udp_session_expire_total`, `udp_batch_calls_total`, `udp_batch_messages_total`, `udp_drop_total`, `udp_reply_primary_total`, `udp_reply_drop_total`, `udp_reply_stale_total`, `udp_active_sessions`
- UDP workers / experimental paths: `udp_worker_packets_in_total`, `udp_worker_packets_out_total`, `udp_worker0_packets_in_total`, `udp_worker1_packets_in_total`, `udp_worker2_packets_in_total`, `udp_worker3_packets_in_total`, `udp_worker0_packets_out_total`, `udp_worker1_packets_out_total`, `udp_worker2_packets_out_total`, `udp_worker3_packets_out_total`, `udp_dataplane_redesign_packets_in_total`, `udp_dataplane_redesign_packets_out_total`, `udp_io_uring_submit_total`, `udp_io_uring_cqe_total`, `udp_io_uring_multishot_total`, `udp_io_uring_buf_release_total`, `udp_io_uring_fallback_total`, `udp_fast_path_packets_in_total`, `udp_fast_path_packets_out_total`, `udp_fast_path_gso_send_total`, `udp_fast_path_gro_recv_total`, `udp_fast_path_fallback_total`, `udp_fast_path_drop_total`

## Error mapping

Current HTTP-level behavior:

- `400 Bad Request` — invalid protocol / host / port / empty legacy update
- `401 Unauthorized` — missing or invalid bearer token
- `404 Not Found` — allocation or binding not found
- `409 Conflict` — no available port
- `503 Service Unavailable` — runtime apply/update/delete timeout or failure
- `500 Internal Server Error` — unexpected internal error
