---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "relayd_port_binding Resource - relayd"
subcategory: ""
description: |-
  Manages the binding attached to a relayd allocation.
---

# relayd_port_binding (Resource)

Manages the binding attached to a relayd allocation.

## Example Usage

```terraform
resource "relayd_port_allocation" "example" {
  protocol = "tcp"
}

resource "relayd_port_binding" "example" {
  allocation_id = relayd_port_allocation.example.id
  host          = "127.0.0.1"
  target_port   = 8080
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `allocation_id` (String) Identifier of the allocation to bind.
- `host` (String) Desired upstream IP literal.
- `target_port` (Number) Desired upstream target port.

### Read-Only

- `created_at_ms` (Number) Creation timestamp in Unix milliseconds.
- `effective_host` (String) Runtime host currently applied by relayd.
- `effective_target_port` (Number) Runtime target port currently applied by relayd.
- `error_kind` (String) Runtime error kind reported by relayd, if any.
- `id` (String) Binding identifier, equal to the allocation identifier.
- `last_error` (String) Last runtime error message reported by relayd, if any.
- `runtime_status` (String) Runtime status reported by relayd.
- `updated_at_ms` (Number) Last update timestamp in Unix milliseconds.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
#!/usr/bin/env sh

set -eu

terraform import relayd_port_binding.example 018f0d5d-86d6-7e57-8bb5-c0b7c38e2f0e
```
